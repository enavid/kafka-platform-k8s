# ==============================================================================
# JKS Password Secret
# Change the value before applying
# Generate with: echo -n "your-password" | base64
# ==============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: kafka-jks-password
  namespace: kafka-platform
type: Opaque
stringData:
  password: "changeit"
